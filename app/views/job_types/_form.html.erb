<%= simple_form_for @job_type, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-info' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                job_types_path, :class => 'btn' %>
  </div>
<% end %>
