<p>Hola <%= @resource.email %>!</p>

<p>Su solicitud para cambiar la contraseña ha sido tramitada exitosamente; puede hacerlo a través del siguiente enlace:</p>

<p><%= link_to 'Cambiar contraseña', edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %></p>

<p>Si recibió este mensaje por error, simplemente elimínelo.</p>