<div class="row-fluid">
	<div class="span12">
		<h2>Regístrate</h2>
		<p>
			Forjando la industria del Software!, Su empresa podrá publicar sus ofertas de empleo totalmente gratis.  
		</p>
	</div>
</div>
<br />
<div class="row-fluid">
	<div class="span3">
		<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
			<% flash.each do |name, msg| %>
				*<%= content_tag :div, msg, :class => 'alert alert-error' %>*
			<% end %>
			<%= f.error_notification %>
			<div class="form-inputs">
				<%= f.input :email, :required => true, :autofocus => true, :input_html => { :class => 'span12' } %>
				<%= f.input :password, :required => true, :input_html => { :class => 'span12' } %>
				<%= f.input :password_confirmation, :required => true, :input_html => { :class => 'span12' } %>
			</div>
			<%= f.button :submit, 'Iniciar sesión', :class => 'btn btn-primary' %>
		<% end %>
		<%= render "devise/shared/links" %>
	</div>
</div>