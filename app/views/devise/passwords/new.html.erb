<h2>He olvidado mi contraseña</h2>
<p>
  Para restablecer su contraseña, introduzca el nombre de usuario (correo electrónico) que utiliza para iniciar sesión.
</p>
<br />
<%= simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
<%= f.error_notification %>
<div class="form-inputs">
	<%= f.input :email, :required => true, :label => 'Ingrese su dirección de correo electronico', :hint => false %>
</div>
<%= f.button :submit, "Enviar instrucciones para restablecer contraseña" %>
<% end %>
<%= render "devise/shared/links" %>
