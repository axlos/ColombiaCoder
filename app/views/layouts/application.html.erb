<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title><%= content_for?(:title) ? yield(:title) : "Colombiandeveloper" %></title>
		<%= csrf_meta_tags %>

		<!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
		<!--[if lt IE 9]>
		<script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
		<![endif]-->

		<%= stylesheet_link_tag "application", :media => "all" %>

		<link href="images/favicon.ico" rel="shortcut icon">
		<link href="images/apple-touch-icon.png" rel="apple-touch-icon">
		<link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
		<link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
	</head>
	<body>
		<div class="navbar navbar-fixed-top">
			<div class="navbar-inner">
				<div class="container">
					<a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
					</a>
					<%= link_to '//<span class="log1"> colombian</span><span class="log2">developer</span>'.html_safe, root_path, :class=> 'logo brand' %>
					<div class="nav-collapse">
						<ul class="nav">
							<li>
								<%= link_to 'Busqueda de ofertas laborales', jobs_path %>
							</li>
						</ul>
						 <!-- The drop down menu -->
						<ul class="nav pull-right">
							<% unless user_signed_in? %>
						        <%= form_for('user', :url => user_session_path, :html => { :class => 'navbar-search pull-left', :style=> 'margin-top: 0px;' }) do |f| %>  
							    	<%= f.text_field :email, :class => 'search-query input-medium', :required => false, :label => false, :placeholder => 'usuario@dominio.com'%>
							    	<%= f.password_field :password, :class => 'search-query input-medium', :required => false, :label => false, :placeholder => 'Contraseña' %>
							    	<%= f.submit 'Iniciar sesión', :class=>"btn" %>
						    	<% end %>
						    	&#243;
						    	<%= link_to 'Crear una cuenta', new_user_registration_path, :class=>"btn btn-mini btn-primary" %>
							<% else %>
								<li class="dropdown">
									<a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-user"></i> <%= current_user.contact.full_name.presence || current_user.contact.email.presence %><b class="caret"></b></a>
									<ul class="dropdown-menu">
										<li>
											<%= link_to "Mis ofertas laborales", admin_jobs_path %>
										</li>
										<li class="divider"></li>
										<li>
											<%= link_to "Cerrar sesión", destroy_user_session_path, :method => :delete %>
										</li>
									</ul>
								</li>
							<% end %>
						</ul>
					</div><!--/.nav-collapse -->
				</div>
			</div>
		</div>

		<div class="container">
			<%= yield %>
		</div>
		
		<!-- /container -->
		
		<footer class="footer">
			<p class="pull-right">
				&copy; Colombiandeveloper 2012
			</p>
		</footer>

		<!-- Javascripts
		================================================== -->
		<!-- Placed at the end of the document so the pages load faster -->
		<%= javascript_include_tag 'jquery', 'rails.validations'-%>
		<%= javascript_include_tag "application" %>
		<% javascript_tag :defer => 'defer' do -%>
		<% end -%>
	</body>
</html>
