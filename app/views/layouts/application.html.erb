<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title><%= content_for?(:title) ? yield(:title) : "Colombiandeveloper" %></title>
		<%= csrf_meta_tags %>

		<!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
		<!--[if lt IE 9]>
		<script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
		<![endif]-->

		<%= stylesheet_link_tag "application", :media => "all" %>

		<link href="images/favicon.ico" rel="shortcut icon">
		<link href="images/apple-touch-icon.png" rel="apple-touch-icon">
		<link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
		<link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
	</head>
	<body>
		<div class="navbar navbar-fixed-top">
			<div class="navbar-inner">
				<div class="container">
					<a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
					</a>
					<a class="brand" href="#">Colombiandeveloper</a>
					<div class="container nav-collapse">
						<ul class="nav">
							<li>
								<%= link_to 'Inicio', root_path%>
							</li>
							<li>
								<%= link_to 'Busqueda de ofertas laborales', jobs_path %>
							</li>
						</ul>
						 <!-- The drop down menu -->
						<ul class="nav pull-right">
							<li class="divider-vertical"></li>
							<% unless user_signed_in? %>
						        <li class="dropdown">
						        	<a href="#" class="dropdown-toggle" data-toggle="dropdown">Iniciar sesión <b class="caret"></b></a>
						            <div class="dropdown-menu" style="padding: 15px; padding-bottom: 0px;">
						            	<%= simple_form_for("user", :url => user_session_path) do |f| %>  
									    	<%= f.input :email, :size=>'30', :required => false %> 
									    	<%= f.input :password, :size=>'30', :required => false %>
											<label class="checkbox">
												<input class="boolean optional" id="user_remember_me" name="user[remember_me]" type="checkbox" value="1">
												<input name="user[remember_me]" type="hidden" value="0">Recordarme
											</label> 
											<%= f.button :submit, :class => 'btn btn-primary' %>
									    	<span class="help-block">
									    		<%= link_to '¿Olvidaste tu contraseña?', new_password_path('user')%>
									    	</span> 	
									    <% end %>  
						            </div>
						        </li>
							<% else %>
								<li>
									<%= link_to (current_user.contact.full_name.presence || current_user.contact.email.presence), edit_contact_path(current_user.contact.id) %>
								</li>
								<li class="dropdown">
									<a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-user"></i><b class="caret"></b></a>
									<ul class="dropdown-menu">
										<li>
											<%= link_to "Mis ofertas laborales", admin_jobs_path %>
										</li>
										<li class="divider"></li>
										<li>
											<%= link_to "Cerrar sesión", destroy_user_session_path, :method => :delete %>
										</li>
									</ul>
								</li>
							<% end %>
						</ul>
					</div><!--/.nav-collapse -->
				</div>
			</div>
		</div>

		<div class="container">
			<%= yield %>
			<footer class="footer">
				<p class="pull-right">
					&copy; Colombiandeveloper 2012
				</p>
			</footer>
		</div>

		<!-- /container -->

		<!-- Javascripts
		================================================== -->
		<!-- Placed at the end of the document so the pages load faster -->
		<%= javascript_include_tag 'jquery', 'rails.validations'-%>
		<%= javascript_include_tag "application" %>
		<% javascript_tag :defer => 'defer' do -%>
		<% end -%>
	</body>
</html>
